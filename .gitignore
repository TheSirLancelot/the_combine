.env
.env.local
data/
logs/
*.db
*.db-wal
*.db-shm
secrets/
.venv/
__pycache__/
*.pyc
.DS_Store
.pytest_cache/
probe_espn_output.txt
probe_*_output.txt

# Yahoo OAuth token cache. Written by the library on every refresh, and it
# holds the client secret and refresh token in a pickle.
.yahoofantasy
*.yahoofantasy
# Rendered snapshots for eyeballing the design. The comment is on its own line
# because git does not support trailing ones: `preview/  # why` is a pattern
# containing spaces and a hash, which matches nothing, which is why these kept
# turning up in `git status` despite apparently being ignored.
preview/

# Where the desktop app drops files it has handed back.
Claude outputs/
